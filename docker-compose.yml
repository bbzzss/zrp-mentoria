services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - database
  database:
    image: mongo
    hostname: mongo
    ports:
      - '27017:27017'
